# String Methods
- string slicing: str[ind_start:ind_end+1]
- changing character cases: str.upper(), str.lower()
- stripping extra spaces: str.strip()
- replacing the first occurence of a string: str.replace(str_to_replace, str_replacement)
- separating string into a list of words: str.split()
